name: hr_payroll
version: 1.0.0
config-version: 2

profile: hr_payroll

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hr_payroll:
    raw:
      +materialized: view
    staging:
      +materialized: view
    marts:
      +materialized: table
